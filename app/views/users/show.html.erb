<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<!-- the size is passed in from user helper -->
				<%= gravatar_for @user, size: 200 %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>

<p> <%= @user.name %></p>